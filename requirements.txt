# CTI War Room — runtime dependencies.
# Pinned to versions verified working on Python 3.12.10 (Phase 0 baseline,
# 2026-06-05): full pytest suite green incl. a real Streamlit AppTest boot
# and a pandas feed-render check.
# Dev/test-only deps (pytest) live in requirements-dev.txt.
#
# NOTE: google-generativeai is END-OF-LIFE (the whole SDK, not just gemini-pro).
#       It is scheduled for removal in Phase 1 (roadmap decision D1); Groq is the
#       active AI engine. Kept here only to lock the current baseline.

streamlit==1.58.0
pandas==3.0.3
requests==2.34.2
beautifulsoup4==4.14.3
feedparser==6.0.12
google-generativeai==0.8.6
duckduckgo-search==8.1.1
aiohttp==3.14.0
python-dateutil==2.9.0.post0
pytz==2026.2
fake-useragent==2.2.0
